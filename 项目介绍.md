# UTAU_VChat 项目介绍

> 这份文档里有几个不同长度的版本，按场合取用：
> 一句话 / 视频简介 / 完整介绍 / 技术亮点。

---

## 一句话版

**让 UTAU 声库"开口说话"的桌面软件 —— DeepSeek 驱动角色对话，
重音テト / 足立レイ 用她们自己的声音念出来。**

---

## 短版（视频简介 / 群介绍，约 150 字）

UTAU_VChat 是一个 Windows 桌面软件，把 UTAU 声库从"会唱歌"变成"会说话"。

你和重音テト或足立レイ聊天，她们会用**自己原本的声库音色**回答你 ——
不是念稿，是带情绪、带语调的自然口语。支持角色切换、情绪语气、
中日双语，还能分析一段参考音频自动学会说话风格。

软件已打包成 1.1 GB 的安装包，**目标电脑不需要装 Python、OpenUtau、
VOICEVOX 或 .NET**，双击装完填个 API Key 就能用。

---

## 完整介绍（GitHub / 发帖用）

### 这是什么

**UTAU_VChat** 是一个把 UTAU 声库变成"会说话的角色"的桌面软件。

UTAU 声库原本是给人调教**唱歌**用的 —— 几百个 wav 采样加上频率文件，
本身没有"说话"这个概念。这个项目做的事，是让它开口说话，
而且是**带语调、带情绪、不机械**的那种。

你和角色聊天，她们用自己原本的音色回答你。

### 现在能做什么

| 功能 | 说明 |
|---|---|
| **多角色** | 重音テト / 足立レイ，各自独立声库、立绘、代表色、调音参数、人格设定 |
| **自然语调** | 不是逐字拼接的机器人腔，有起伏、有停顿、有轻重 |
| **情绪语气** | 自动判断回复的情绪（开心/生气/悲伤/温柔/惊讶/傲娇），动态调整音高、语域、语速、气声、紧张度 |
| **角色人格** | 基于萌娘百科设定的角色卡，含人物关系网，不容易出戏 |
| **中日双语** | 说话语言与显示语言可分离 —— 可以"说日语、显示中文字幕" |
| **参考音频学习** | 丢一段音频进来，自动分析声学特征并推荐调音参数 |
| **两种界面** | GUI（角色立绘背景）或 CLI（终端风格） |

### 怎么用

1. 下载安装包（673 MB），双击安装
2. 第一次启动填写**你自己的** DeepSeek API Key（软件不附带任何密钥）
3. 在输入框打字回车，角色就用 UTAU 声库念出来

### 技术上有意思的地方

**核心创意：用一个 TTS 当另一个合成器的"语调老师"。**

UTAU 声库只提供音色，不提供"这句话该怎么说"。自己写规则会很生硬，
所以这个项目借 **VOICEVOX**（成熟的日语语音合成）先把句子念一遍，
提取它的**音高曲线和音节时长**，再翻译成 UTAU 引擎能接受的音符，
最后交给 **OpenUtau** 的 WORLDLINE-R 神经声码器渲染。

```
文本 ──► VOICEVOX 念一遍 ──► 提取音高曲线 + 时长
                                    │
                                    ▼
                            翻译成 UTAU 音符（音高/滑音/长度）
                                    │
                                    ▼
                            OpenUtau 渲染 ──► 音频
```

这是它听起来不像传统拼接 TTS 的关键。

**工程上的几个坎：**

- **把 OpenUtau 从 GUI 里剥出来** —— OpenUtau 是个图形化编辑器，
  这个项目只想要它的合成能力。写了个 368 行的 C# 渲染器直接调它的
  `OpenUtau.Core`，不启动任何界面。
- **让目标电脑零依赖** —— 安装包自带 Python 运行时、.NET 8 运行时、
  OpenUtau 引擎、VOICEVOX 模型、日语词典、两个声库，合计 1.1 GB。
  对方的电脑上什么都不用装。
- **中文发音** —— UTAU 声库只有日语五十音，中文靠 pypinyin 转拼音、
  再用假名近似发音并映射声调（这条还在施工中）。

### 项目规模

| | |
|---|---|
| 代码 | 3652 行（Python 2651 / C# 368 / 构建脚本 633） |
| 文档 | 约 3 万字（README、架构文档、使用说明、更新预告） |
| 发布包 | 673 MB 安装程序 / 770 MB 绿色版 |
| 平台 | Windows 10 / 11 x64 |

### 关于版权

软件原创代码采用 MIT 许可证。随包分发的第三方组件
（OpenUtau、.NET 运行时、VOICEVOX CORE、NumPy、Pillow 等）
版权归各自作者所有，完整的组件清单与许可证原文在 `licenses/` 目录下。

**声库有独立的使用条款**（重音テト / 足立レイ / VOICEVOX 语音模型），
不适用本软件的许可证，使用与再分发前请自行确认。

---

## 技术亮点（给懂行的人看）

如果你要跟有技术背景的人讲这个项目，这几个点最值得说：

**1. 「跨引擎知识迁移」**
用 VOICEVOX 的音高预测能力去驱动 UTAU 的采样合成。
两个引擎的音素体系、时长单位、音高表示完全不同，
中间做了一层映射（音高曲线 → UTAU 的 pitch point，时长 → tick，
音节 → 假名/音素）。这一步是项目的地基。

**2. 「无头化 GUI 应用」**
OpenUtau 是个 Avalonia 桌面程序，官方没有任何命令行或 API。
通过引用 `OpenUtau.Core.dll` + `OpenUtau.Plugin.Builtin.dll`，
自己初始化 `DocManager` / `ToolsManager` / `PlaybackManager`，
构造 `UProject` 对象树，绕开全部 UI 直接调用 `RenderMixdown`。

**3. 「用声学指纹做说话人分离」**
分析多角色参考视频时，需要先判断"这句话是谁说的"。
试过几种特征后，**抖动率（jitter，周期间隔的不规则程度）** 分离度最好：
实测足立レイ 8.6~12.8%、重音テト 2.3~3.9%，差三倍以上，
拿它当判别依据基本不会认错人。

整套声学分析是纯 numpy 写的（FFT 自相关测 F0、HNR 谐噪比、
26 组 mel 滤波器 + DCT 算 MFCC），没有依赖 librosa 或 praat-parselmouth ——
因为要打进 1.1 GB 的安装包里，每多一个依赖都是负担。

**4. 「踩过的坑都记在案」**
`ARCHITECTURE.md` 里有一张已知问题表，每条都写了现象、根因、修法。
比较有意思的两个：

- **中文输入导致 API 返回 400** —— Windows 中文环境下 `sys.stdin`
  是 `gbk + surrogateescape`，UTF-8 字节被解出孤立代理项，
  塞进 JSON 直接被拒。
- **装在 `C:\Program Files` 后能启动但不出声** —— OpenUtau 把渲染缓存
  写在引擎目录下，只读时它**不报错也不产出**，静默失败。
  这个坑查了挺久。

---

## 关于「第一个项目」

如果你要介绍给别人，可以这么说：

> 这是我的第一个项目。从一个很具体的想法开始 ——
> 「重音テト能不能用她自己的声音跟我说话」——
> 然后一路做到能打包安装、能给别人用的完整软件。

从工程角度看，这确实不是个玩具项目：

- **有完整的交付链** —— 源码、构建脚本、安装程序、绿色版、
  MIT 许可证、第三方合规清单、用户文档，该有的都有
- **有桌面软件该有的东西** —— 首次运行引导、配置持久化、
  自检命令、错误提示、卸载清理
- **有认真处理过边界情况** —— 只读目录、无管理员权限、
  中文编码、多语言文件名、缓存污染

更难得的是**过程里的判断**：什么时候该借别人的轮子（VOICEVOX 当语调老师）、
什么时候该自己动手（把 OpenUtau 剥壳）、
什么时候该承认做不到（中文发音还在施工、流式合成不做）。

第一个项目能做到「自己用着舒服、别人拿去也能装上用」，
已经是很好的起点了。

---

## 用词建议

对外介绍时，这几个说法比较准确：

| ✅ 建议这么说 | ❌ 别说成 |
|---|---|
| 让 UTAU 声库"说话" | 语音合成引擎（会让人以为是自己写的声码器） |
| 借 VOICEVOX 预测语调 | 用了 VOICEVOX 的声音（用的是语调，不是音色） |
| 用 OpenUtau 的合成能力 | 我写了 OpenUtau / 我做了声码器 |
| 基于 DeepSeek 的角色对话 | 自己训练了语言模型 |
| 参考音频学习（分析声学特征后推荐参数） | AI 声音克隆（不是克隆，是调参） |

最后一条特别重要 —— **「声音克隆」和「参考音频调参」是两回事**，
说成前者容易引起误会。
