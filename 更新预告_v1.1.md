# UTAU_VChat v1.1 更新预告

> 当前版本 **v1.0.0**　→　预告版本 **v1.1.0**（开发中，功能与时间可能调整）

v1.0 让 UTAU 声库能"开口说话"。v1.1 要做的是让它**能被别的软件调用**，
并且**不再只认识两个歌姬**。

这次三个主要方向：

| 方向 | 一句话 |
|---|---|
| **语音 API 中转模式** | 把 UTAU_VChat 变成语音后端，AIRI 等外部软件说文本、拿回 UTAU 语音 |
| **自定义角色卡** | 角色不再写死在 `characters.json`，界面上直接增删改 |
| **自定义 UTAU 歌姬** | 丢一个声库文件夹进来就能用，图标自动找、自动转 |

---

## 一、语音 API 中转模式

### 要解决什么

现在的流程是「UTAU_VChat 自己聊天 → 自己发声」，是个闭环。
但很多人已经有自己的对话前端了 —— 比如 [AIRI](https://github.com/moeru-ai/airi)，
它负责角色形象、对话逻辑、语音识别，声音则交给外部 TTS 服务商。

UTAU_VChat 接进去之后，一次对话是这样的：

```
  AIRI                        UTAU_VChat
   │                              │
   │  ① 角色回复文本               │
   │  POST /v1/audio/speech       │
   │  {"input": "...", "voice": "teto"}
   ├─────────────────────────────►│
   │                              │  ② 用 UTAU 声库渲染
   │                              │     （重音テト / 足立レイ）
   │  ③ WAV 音频（HTTP 响应体）     │
   │◄─────────────────────────────┤
   │                              │
   ▼
  AIRI 的角色用重音テト的声音说话
```

AIRI 那边**不需要装插件、也不用改代码** —— 它本来就支持填自定义 `baseUrl`，
把地址指向本机的 UTAU_VChat 就行。

### 接口设计

按 OpenAI 的语音接口做，这样兼容的不只是 AIRI：

```
POST /v1/audio/speech
{
  "model":   "tts-1",             // 随便传，接口会忽略
  "input":   "こんにちは、テストです。",
  "voice":   "teto",              // ← 歌姬 id，或自定义歌姬的名字
  "speed":   1.15,                // 可选：吐字速度
  "response_format": "wav"
}
→ 200 OK, audio/wav（直接返回音频字节）
```

配套几个查询接口：

```
GET /v1/models          列出可用歌姬（AIRI 的下拉框会读它）
GET /v1/voices          同上，带立绘/头像信息
GET /health             存活检查
```

### 怎么接

AIRI 的语音合成走的是 `unspeech` 统一层，各服务商都支持填自定义 `baseUrl`
（[官方配置说明](https://airi.moeru.ai/docs/zh-Hans/docs/manual/config/)）。
所以选「OpenAI 兼容」，把地址指过来就行：

```json
{
  "apiKey": "local",
  "baseUrl": "http://127.0.0.1:17300/v1/"
}
```

歌姬在 AIRI 的角色卡里选 `teto` 或 `adachi`，就完事了。
**AIRI 那边不需要装任何插件，也不用改代码。**

### 几个会一起做的细节

- **常驻渲染进程**：现在每次合成都启动一次 `ou_render.exe`。
  实测纯启动开销约 **0.45 秒**（下面表格里"缓存命中"那一列就是它的下限），
  引擎常驻后这部分可以省掉 —— 在连续对话里累积起来很可观。
- **长文本自动切句**：按标点切成多句分别渲染，
  这样**第一句合成完就能先播**，不用等整段渲染完。
- **请求级参数覆盖**：不想改全局设置的话，
  可以在请求里带上 `speed` / `tone` / `emotion`，只对这一次生效。
- **只监听本机**：默认绑 `127.0.0.1`，不对外网开放。
  需要局域网访问得手动改配置，并会强制要求一个本地 token。
- **情绪联动（可选）**：请求里可以带 `emotion` 字段，
  直接指定「开心/生气/温柔」等语气，跳过自动情绪判断。
- **`model` 字段会被忽略**：AIRI 可能会传 `tts-1` 之类的值，
  接口一律忽略，只看 `voice` 和 `input`。

### 性能：实测数据

在本机（重音テト，12 代 i7 级别 CPU，纯 CPU 推理）实测：

| 文本长度 | 音频时长 | 首次渲染 | 相对实时 | 缓存命中 |
|---|---|---|---|---|
| 一句短回复（15 拍） | 2.9 秒 | 1.40 秒 | **2.1× 实时** | 0.45 秒 |
| 典型回复（44 拍） | 8.2 秒 | 3.33 秒 | **2.5× 实时** | 0.48 秒 |
| 较长回复（81 拍） | 14.4 秒 | 5.96 秒 | **2.4× 实时** | 0.50 秒 |

换算下来约 **每拍 70 毫秒**，基本是线性的。

结论：

- **回合制语音对话完全够用** —— AIRI 的回复通常 5~10 秒音频，
  合成等 2~4 秒，比听它说完还快。
- **重复的台词会命中缓存**，几乎瞬间返回。
- 需要「边想边说」的**流式**场景不行 —— 那是另一个量级的工作，
  v1.1 不做，也不打算假装能做。

---

## 二、自定义角色卡

### 现在的问题

角色写死在 `characters.json` 里，加一个角色要手改 JSON、
自己算路径、自己写人格提示词。普通用户做不来。

### v1.1 的改法

界面上直接管理，`/set` 里加一个 `character` 分组：

```
你 > /set char list
  1. 重音テト     (teto)    声库: 重音テト OU用日本語統合ライブラリー
  2. 足立レイ     (adachi)  声库: 足立レイver3.5.0
  3. 桃音モモ     (momo)    声库: 桃音モモ連続音          ← 自建

你 > /set char new momo
  正在创建角色卡... 请依次填写（回车跳过用默认值）
  显示名        : 桃音モモ
  声库文件夹    : （自动扫描中...）
  代表色 #rrggbb: #ffb3d9
  人格提示词    : （可留空，之后用 /set char edit momo 补）
  已创建 → characters.json
```

角色卡包含：

| 字段 | 说明 |
|---|---|
| 显示名 / id | 界面上显示的名字 |
| 声库文件夹 | 指向 `singers/` 下任意声库 |
| 立绘 / 图标 | 可自定义，留空则自动找（见下一节） |
| 代表色 | GUI 里说话人标签的颜色 |
| 人格提示词（日/中） | 两份，跟随说话语言自动切换 |
| 人物关系 | 角色之间的称呼与互动设定 |
| 调音参数 | 音高、音域、语速、停顿、气声、紧张度、辅音速度 |
| 情绪开关 | 是否启用情绪演绎 |

另外会支持**角色卡导入导出**（单个 `.json` 文件），
方便互相分享角色设定 —— 分享的是人格与参数，不含声库本体。

---

## 三、自定义 UTAU 歌姬（自动图标）

### 现在的问题

加新歌姬要手动翻声库目录、找 `character.txt`、
再把立绘和图标路径填进 JSON。图标还得自己转格式。

### v1.1 的改法：丢进来就能用

```
你 > /set singer add "D:\声库\重音テト OU用日本語統合ライブラリー"
  扫描声库...
    ✓ 识别为 UTAU 声库（character.txt）
    名称      : 重音テト
    作者      : 線路は続くよ
    音源数    : 13 个子库
    图标      : teto.bmp          ← 自动找到
    立绘      : official_illust_teto_new.png
    试渲染    : OK（1.4 秒，15 拍）
  已加入歌姬列表 → 可在 /set voice 里选择
```

会自动做的事：

1. **识别格式** —— UTAU（`character.txt`）与 OpenUtau（`character.yaml`）都认
2. **读取元信息** —— 名称、作者、网页、版本、音源数量
3. **自动找图标**，按优先级：
   - `character.yaml` 里 `image:` 指定的图
   - 声库根目录的 `icon.bmp` / `avatar.png` / `*.bmp`
   - `character.txt` 里 `image=` 指定的图
   - 立绘裁切（找不到专门图标时）
4. **自动转成多尺寸 `.ico`**（16/24/32/48/64/128/256），
   并设为**窗口、任务栏、Alt-Tab 的图标** ——
   也就是切到哪个歌姬，任务栏图标就跟着换
5. **试渲染一句**，确认这个声库真的能用（避免加进来才发现是坏的）
6. **自动判断音源类型**，选择合适的音素器

第 4 步的图标管线 v1.0.0 里已经做好了（`make_icon_ico()`），
v1.1 只是把它接到"自动找图"上。

---

## 四、顺手会做的小改进

- `/set` 面板支持**分组折叠**，项目多了不至于刷屏
- 设置支持**导出/导入**，换电脑不用重配
- **渲染进度提示**：长句子合成时显示进度，而不是干等
- **缓存可视化与清理**：引擎其实已经在缓存渲染结果（实测重复台词
  从 3.3 秒降到 0.48 秒），但用户看不见也清不掉 ——
  会加一个「缓存占用 / 清空缓存」的入口
- 首次启动引导里加一步「**要不要顺便接进 AIRI**」的说明与一键配置

---

## 五、时间与状态

| 阶段 | 内容 | 状态 |
|---|---|---|
| 设计 | 接口形状、角色卡 schema、声库扫描规则 | 进行中 |
| 实现 | API 服务 + 常驻渲染进程 | 计划中 |
| 实现 | 角色卡增删改 + 导入导出 | 计划中 |
| 实现 | 声库扫描 + 图标自动生成 | 计划中 |
| 联调 | 与 AIRI 实际对接测试 | 计划中 |

v1.0.0 已经可以正常使用（[下载与说明见 README](README.md)），
v1.1 会以**覆盖升级**的方式发布，设置和角色卡自动沿用。

---

## 六、想要什么功能，现在说还来得及

上面这些是按"让 UTAU 声库能被更多场景用上"这条线排的。
如果你有别的想法 —— 比如：

- 想接的前端不是 AIRI，而是别的（Discord 机器人？直播弹幕？）
- 自定义角色卡里还想要什么字段
- 声库图标想用立绘的某个部位而不是整张图

现在提出来，比做完了再改容易得多。

---

## 相关链接

- AIRI 项目：<https://github.com/moeru-ai/airi>
- AIRI 语音合成配置：<https://airi.moeru.ai/docs/zh-Hans/docs/manual/config/>
- OpenAI 语音接口形状（本项目的兼容目标）：
  `POST /v1/audio/speech`，请求体含 `model` / `input` / `voice` / `response_format`
